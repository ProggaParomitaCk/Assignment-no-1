var numbers = [12, 65, 67, 89, 90, 122];
function getOddNumbersOfAnArray(numbers) {
    for (let i = 0; i < numbers.length; i++) {
        const index = i;
        const element = numbers[index];
        if (elements % 2 !== 0) {
            console.log(index, element);
        }

    }
}